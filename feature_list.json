[
  {
    "id": "harness-setup",
    "area": "meta",
    "title": "Set up Claude Code harness files",
    "behavior": "CLAUDE.md, claude-progress.md, and feature_list.json exist at repo root — CLAUDE.md routes to topic docs, claude-progress.md tracks verified state, feature_list.json is the machine-readable task primitive",
    "state": "passing",
    "priority": "high",
    "criteria": [
      "CLAUDE.md exists at repo root with conventions, test commands, and definition of done",
      "claude-progress.md exists and tracks current verified state",
      "feature_list.json exists with this schema"
    ],
    "verification": "ls CLAUDE.md claude-progress.md feature_list.json",
    "evidence": "commit 54fb9ee1523 — 2026-06-13; ls exits 0"
  },
  {
    "id": "install-postgresql",
    "area": "infrastructure",
    "title": "Install PostgreSQL and initialise odoo_dev database",
    "behavior": "PostgreSQL 17 is installed and reachable at localhost:5432; odoo role exists; odoo_dev database is initialised; ./verify.sh account exits 0 end-to-end",
    "state": "passing",
    "priority": "high",
    "criteria": [
      "psql --version returns a version string",
      "psql -U odoo -c \"SELECT 1;\" succeeds",
      "odoo_dev database exists",
      "./verify.sh account exits 0 (full pipeline green)"
    ],
    "verification": "./verify.sh account",
    "evidence": "commit 8aa75da87ea — 2026-06-13; ./verify.sh account: pre-flight ok, 0 failed tests"
  },
  {
    "id": "up5-nsvn-cylinder",
    "area": "nsvn-mvp",
    "title": "NSVN MVP — Cylinder Lifecycle Tracking",
    "behavior": "The up5_nsvn_cylinder module installs on Odoo 19; adds cylinder-specific fields to stock.lot (type, capacity, customer, dispatch date, rental period); computes overdue status and days-at-customer; shows a dedicated Cylinders list view under Inventory with overdue rows highlighted in red; ./verify.sh up5_nsvn_cylinder exits 0",
    "state": "active",
    "priority": "high",
    "criteria": [
      "product.template extended with is_cylinder (Boolean) and gas_type (Selection: O2-industrial, O2-medical, N2, Ar, CO2, H2, He, mixed, other)",
      "stock.lot extended with cylinder_capacity, cylinder_pressure_rating, cylinder_manufacture_year, cylinder_next_inspection, cylinder_state (available/at_customer/filling/maintenance/retired), customer_id (Many2one res.partner), dispatch_date, rental_days (default 30)",
      "Computed fields on stock.lot: expected_return_date, days_at_customer, is_overdue, overdue_days — all store=True with @api.depends",
      "stock.lot form view extended with a Cylinder tab showing all cylinder fields",
      "product.template form view extended with is_cylinder + gas_type fields",
      "Cylinders menu item under Inventory with a list view filtered to is_cylinder=True; overdue rows decoration-danger",
      "TransactionCase tests: overdue cylinder, not-overdue cylinder, no-customer cylinder (failure scenario), dispatch-without-customer no-overdue (failure scenario)",
      "./verify.sh up5_nsvn_cylinder exits 0 — paste full output as evidence"
    ],
    "verification": "./verify.sh up5_nsvn_cylinder",
    "evidence": ""
  }
]
